Google Formsに届いた問い合わせを、複数フォーム横断で一括管理できるWebアプリを作成してください。

既存プロジェクトが存在する場合は、最初にディレクトリ構成、既存ファイル、使用技術、環境変数、既存機能を確認してください。
既存機能や既存ファイルを勝手に削除せず、必要な箇所だけを安全に変更してください。

アプリ名は仮で「返事きたで」とします。

==================================================
1. 背景
==================================================

現在、複数のWebサービスでGoogle Formsを問い合わせフォームとして利用しています。

例：

・u-shin
・はよおきんかい
・まとめときや

Google Formsのトップ画面にはフォームが一覧表示されていますが、新しい問い合わせを確認するためには、それぞれのフォームを1つずつ開き、「回答」タブへ移動する必要があります。

フォームが増えるほど、以下の問題が発生します。

・どのフォームに新しい回答が届いたのか分からない
・フォームを毎回1つずつ開くのが面倒
・複数フォームの回答をまとめて確認できない
・未確認か確認済みか分からない
・未対応か返信済みかを管理しにくい
・対応漏れが発生する
・過去の問い合わせを探しにくい
・フォームごとに画面を行き来する必要がある

この問題を解決するため、複数のGoogleフォームに届いた回答を、メールの受信箱のように1つの画面で確認・管理できるWebアプリを作成します。

==================================================
2. アプリの目的
==================================================

複数のGoogleフォームに届いた回答を、フォーム単位ではなく「届いた回答単位」で新しい順にまとめて表示してください。

アプリを開いた直後に、管理対象のすべてのフォームへ届いた問い合わせを一括確認できるようにします。

アプリの説明文は以下です。

「複数のGoogleフォームに届いた問い合わせを、一括確認・対応管理できるWebアプリ」

このアプリの中心的な価値は、Google Forms自体を作成しやすくすることではありません。

以下を実現することが目的です。

・複数フォームの回答をまとめて確認する
・未読と既読を管理する
・未対応、対応中、対応済み、保留を管理する
・重要な問い合わせを分かりやすくする
・管理者用メモを残す
・問い合わせの対応漏れを防ぐ
・Google Formsを毎回1つずつ開く作業をなくす

==================================================
3. 使用技術
==================================================

以下の技術構成で実装してください。

・バックエンド：Python
・Webフレームワーク：Flask
・フロントエンド：HTML、CSS、JavaScript
・テンプレートエンジン：Jinja2
・データベース：Firebase Cloud Firestore
・Firebase操作：Firebase Admin SDK for Python
・ログイン認証：Google OAuth 2.0
・Googleフォーム一覧取得：Google Drive API
・Googleフォーム情報取得：Google Forms API
・Googleフォーム回答取得：Google Forms API
・CSRF対策：Flask-WTF
・環境変数管理：python-dotenv
・本番環境：Ubuntu VPS
・アプリサーバー：Gunicorn
・Webサーバー：Nginx

以下は使用しないでください。

・SQLite
・MySQL
・PostgreSQL
・Flask-SQLAlchemy
・SQLAlchemy
・React
・Next.js
・Vue.js

Firebase Authenticationは必須ではありません。

ログイン処理はGoogle OAuth 2.0を使用し、Google Drive APIとGoogle Forms APIへアクセスできるようにしてください。

Googleログインで取得したユーザー情報と、アプリ独自の管理データはCloud Firestoreに保存してください。

==================================================
4. MVPで実装する機能
==================================================

最初に以下の機能を実装してください。

1. Googleアカウントでログイン
2. Googleフォーム一覧を取得
3. 管理対象フォームを選択
4. 複数フォームの回答をまとめて取得
5. すべての回答を新しい順に一覧表示
6. 未読と既読を管理
7. 対応状況を管理
8. 重要マークを管理
9. 管理者用メモを保存
10. 検索と絞り込み
11. 最新回答を手動で同期
12. Google Formsの回答との重複を防止

==================================================
5. Googleログイン
==================================================

Google OAuth 2.0を使用してログインできるようにしてください。

ログインしていないユーザーにはログイン画面を表示してください。

ログイン画面には以下を表示してください。

・アプリ名「返事きたで」
・アプリの説明
・Googleでログインするボタン
・取得する情報についての簡単な説明
・プライバシーに関する案内

Googleログイン後は以下の情報を取得してください。

・GoogleユーザーID
・名前
・メールアドレス
・プロフィール画像URL

取得したユーザー情報はCloud Firestoreに保存してください。

OAuthのstateを検証し、CSRF攻撃を防止してください。

ログイン後のセッションには、最低限以下を保存してください。

・ログインユーザーを特定するID
・Google OAuthの認証状態

OAuthクライアントIDやクライアントシークレットをソースコードへ直接書かないでください。

==================================================
6. Googleフォーム一覧の取得
==================================================

Google Drive APIを利用して、ログインユーザーが所有またはアクセスできるGoogleフォームを取得してください。

Googleフォームだけを対象にしてください。

フォーム一覧画面には以下を表示してください。

・フォーム名
・フォームID
・最終更新日時
・管理対象かどうか
・最終同期日時
・取得済み回答数
・未読件数
・未対応件数
・管理対象へ追加するボタン
・管理対象から外すボタン
・元のGoogleフォームを開くボタン

初回ログイン時、または管理対象フォームが0件の場合は、管理するGoogleフォームを選択する画面を表示してください。

チェックボックスで複数のフォームを選択し、「管理対象に追加」を押すと登録できるようにしてください。

管理対象から外した場合でも、すぐにFirestore内の過去データを完全削除しない設計にしてください。

ManagedFormのis_activeをfalseにして、同期対象から除外してください。

==================================================
7. 複数フォームの回答を一括表示
==================================================

管理対象として登録した複数のGoogleフォームから、Google Forms APIを使用して回答を取得してください。

取得した回答は、フォームごとに分けて表示するのではなく、すべてのフォームをまとめて新しい順に並べてください。

一覧には以下を表示してください。

・フォーム名
・回答者名
・回答者のメールアドレス
・問い合わせ内容の一部
・回答日時
・未読または既読
・対応状況
・重要マーク
・管理メモがあるかどうか

フォームごとに質問項目や質問名が異なるため、以下のような特定の質問名だけに依存しない設計にしてください。

・お名前
・氏名
・名前
・メール
・メールアドレス
・お問い合わせ
・お問い合わせ内容
・ご意見
・フィードバック

すべての質問タイトルと回答内容を保持してください。

名前、メールアドレス、問い合わせ本文として表示する値については、質問タイトルを確認して推測してください。

該当する質問が見つからない場合もエラーにせず、以下のような表示にしてください。

・回答者名：名前なし
・メールアドレス：取得できません
・問い合わせ内容：回答内容の先頭部分

回答の詳細画面では、すべての質問と回答を表示してください。

==================================================
8. 受信箱形式のトップ画面
==================================================

トップページはフォーム一覧ではなく、問い合わせの受信箱を表示してください。

デスクトップ版は、以下の3カラム構成を基本にしてください。

左側：
・ナビゲーションとフォーム絞り込み

中央：
・問い合わせ一覧

右側：
・選択した問い合わせの詳細

画面全体の構成は以下です。

--------------------------------------------------
左サイドバー
--------------------------------------------------

・すべての問い合わせ
・未読
・未対応
・対応中
・対応済み
・保留
・重要
・フォームごとの絞り込み
・フォーム管理
・設定
・ログアウト

各項目の横には件数を表示してください。

例：

すべて　　　　 25
未読　　　　　 3
未対応　　　　 5
対応中　　　　 2
対応済み　　　17
保留　　　　　 1
重要　　　　　 2

--------------------------------------------------
中央の問い合わせ一覧
--------------------------------------------------

各問い合わせをカードまたはリスト形式で表示してください。

表示項目：

・未読マーク
・フォーム名
・回答者名
・問い合わせ本文の一部
・回答日時
・対応状況のバッジ
・重要マーク
・メモの有無

未読の回答は、既読の回答よりも目立つようにしてください。

例：

・文字を太字にする
・左側に丸い未読マークを付ける
・薄い背景色を付ける

一覧をクリックすると、ページ全体を移動せずに右側へ詳細を表示してください。

--------------------------------------------------
右側の回答詳細
--------------------------------------------------

以下を表示してください。

・フォーム名
・回答日時
・回答者名
・回答者メールアドレス
・すべての質問と回答
・未読、既読の状態
・対応状況変更ボタン
・重要マーク
・管理者用メモ
・元のGoogleフォームを開くボタン
・元の回答データを確認するための情報

問い合わせの詳細を開いたタイミングで、その問い合わせを既読に変更してください。

スマートフォンでは3カラムではなく、一覧から詳細画面へ移動する形式でも構いません。

==================================================
9. ダッシュボード上部
==================================================

トップ画面の上部には、以下の情報をカード形式で表示してください。

・今日届いた回答数
・未読件数
・未対応件数
・対応中件数
・対応済み件数
・重要件数
・最終同期日時

右上には「回答を更新」ボタンを設置してください。

更新中は以下を表示してください。

・ローディングアイコン
・「回答を取得しています」という文言
・ボタンの連続クリック防止

更新完了後は、以下のようなメッセージを表示してください。

「3件の新しい回答を取得しました」

新しい回答がなかった場合は、以下を表示してください。

「新しい回答はありませんでした」

==================================================
10. 未読・既読管理
==================================================

Google Forms側ではなく、このアプリ独自の管理データとして未読と既読を保存してください。

新しい回答を初めてFirestoreへ登録したときは、以下を初期値にしてください。

is_read：false

ユーザーが問い合わせ詳細を開いたら、以下へ更新してください。

is_read：true

未読状態はGoogle Forms側の回答データを再同期しても上書きしないでください。

同期処理によって、ユーザーが設定した以下の値が初期状態へ戻らないようにしてください。

・is_read
・status
・is_important
・admin_memo

==================================================
11. 対応状況管理
==================================================

各回答に以下の対応状況を設定できるようにしてください。

・未対応
・対応中
・対応済み
・保留

Firestoreに保存する値は以下を使用してください。

・未対応：unhandled
・対応中：in_progress
・対応済み：completed
・保留：on_hold

新しく取得した回答の初期状態は以下です。

status：unhandled

一覧画面と詳細画面の両方から、ステータスを変更できるようにしてください。

ステータス変更時は、ページ全体を再読み込みせずに更新できるようにしてください。

変更に成功した場合は、画面上の件数も更新してください。

==================================================
12. 重要マーク
==================================================

各問い合わせに重要マークを設定できるようにしてください。

初期値は以下です。

is_important：false

重要マークを押すとtrueとfalseを切り替えられるようにしてください。

重要な問い合わせだけをサイドバーから絞り込めるようにしてください。

==================================================
13. 管理者用メモ
==================================================

各回答に、管理者だけが確認できるメモを保存できるようにしてください。

例：

・7月15日に返信予定
・不具合を確認中
・次回アップデートで対応
・迷惑問い合わせの可能性あり
・開発担当へ確認する

メモはGoogle Forms側へ書き戻さず、Firestoreだけに保存してください。

自動保存または保存ボタンを実装してください。

自動保存を使用する場合は、入力するたびに保存せず、一定時間入力が止まってから保存してください。

保存中、保存完了、保存失敗の状態を表示してください。

==================================================
14. 検索と絞り込み
==================================================

以下の条件で問い合わせを絞り込めるようにしてください。

・フォーム名
・対応状況
・未読または既読
・重要のみ
・回答者名
・メールアドレス
・問い合わせ本文
・回答期間
・新しい順
・古い順

検索欄に文字を入力すると、以下を対象に検索してください。

・回答者名
・メールアドレス
・質問タイトル
・回答内容
・管理者用メモ

Cloud Firestoreには標準の全文検索機能がないため、MVPでは以下の方針にしてください。

・フォームIDによる絞り込みはFirestoreクエリを使用
・statusによる絞り込みはFirestoreクエリを使用
・is_readによる絞り込みはFirestoreクエリを使用
・is_importantによる絞り込みはFirestoreクエリを使用
・回答期間による絞り込みはFirestoreクエリを使用
・文字列検索は取得済みデータをFlask側またはフロント側で絞り込む
・最初からAlgoliaなどの外部全文検索サービスは導入しない

データ量が増えた場合に、全文検索サービスへ移行しやすい構成にしてください。

==================================================
15. 手動同期
==================================================

ヘッダーに「回答を更新」ボタンを設置してください。

ボタンを押すと、管理対象となっているすべてのGoogleフォームから最新回答を取得してください。

同期処理では以下を行ってください。

1. 管理対象フォームを取得
2. 各フォームの最新回答をGoogle Forms APIから取得
3. Google FormsのresponseIdを確認
4. Firestoreに存在しない回答だけを新規登録
5. 既存回答は重複登録しない
6. ManagedFormのlast_synced_atを更新
7. 新しく登録した件数を返す
8. 同期に失敗したフォームがあれば記録する

一部のフォームの同期に失敗した場合でも、すべての処理を中断しないでください。

例：

「3件の新しい回答を取得しました。1件のフォームで取得に失敗しました」

取得済み回答のstatus、is_read、is_important、admin_memoは同期によって上書きしないでください。

==================================================
16. Firestoreのデータ設計
==================================================

Cloud Firestoreには、以下のコレクション構成でデータを保存してください。

users/{userId}

フィールド：

・google_user_id
・email
・name
・picture_url
・created_at
・updated_at

users/{userId}/managed_forms/{formId}

フィールド：

・google_form_id
・title
・is_active
・response_count
・unread_count
・unhandled_count
・last_synced_at
・created_at
・updated_at

users/{userId}/managed_forms/{formId}/responses/{responseId}

フィールド：

・google_response_id
・respondent_email
・respondent_name
・summary_text
・search_text
・submitted_at
・answers
・is_read
・status
・is_important
・admin_memo
・created_at
・updated_at

Google FormsのformIdを、managed_formsのドキュメントIDとして使用してください。

Google FormsのresponseIdを、responsesのドキュメントIDとして使用してください。

これにより、同じ回答を再取得しても重複登録されないようにしてください。

answersには、Google Forms APIから取得したすべての質問タイトルと回答内容を保存してください。

例：

{
  "question_id_1": {
    "question_id": "question_id_1",
    "question": "お名前",
    "answer": "山田太郎"
  },
  "question_id_2": {
    "question_id": "question_id_2",
    "question": "お問い合わせ内容",
    "answer": "ログインできません"
  }
}

複数選択、チェックボックス、日時、ファイル、評価など、回答形式が異なる場合も保存できる構造にしてください。

回答が複数存在する場合は配列として保存してください。

例：

{
  "question": "希望する機能",
  "answer": [
    "通知機能",
    "ダークモード"
  ]
}

Firestoreの日付データには、可能な限りFirestore Timestampを使用してください。

日時を文字列だけで保存しないでください。

==================================================
17. Firestoreの件数管理
==================================================

ManagedFormには以下の集計値を保存してください。

・response_count
・unread_count
・unhandled_count

ただし、集計値だけを完全な正解として信用しない設計にしてください。

回答の追加やステータス変更時に、必要に応じてトランザクションまたは安全な更新処理を使用してください。

集計値にずれが発生した場合に再計算できる処理も用意してください。

全フォームを横断した件数は、各フォームの集計値を合計するか、必要に応じて回答を集計してください。

==================================================
18. Firebase初期化
==================================================

Firebase Admin SDKを使用してCloud Firestoreへ接続してください。

Firebase初期化処理は複数ファイルに重複して書かず、専用ファイルへまとめてください。

例：

app/firebase_config.py

このファイルの役割：

・Firebase Admin SDKの初期化
・Firestoreクライアントの生成
・Firebase初期化の重複防止
・環境変数の確認
・サービスアカウントJSONの読み込み
・認証情報が存在しない場合のエラー処理
・MOCK_MODE時はFirebaseへ接続しない処理

Firebase Admin SDKがすでに初期化されている場合、再初期化しないでください。

==================================================
19. Repository構成
==================================================

Firestoreへの読み書きを、ルート処理やGoogle API処理へ直接大量に書かないでください。

Repository層へ分離してください。

最低限、以下のRepositoryを作成してください。

・UserRepository
・ManagedFormRepository
・FormResponseRepository

役割例：

UserRepository

・ユーザーの作成
・ユーザー情報の更新
・ユーザー情報の取得

ManagedFormRepository

・管理対象フォームの登録
・管理対象フォームの取得
・is_activeの変更
・最終同期日時の更新
・件数の更新

FormResponseRepository

・回答の新規登録
・responseIdによる存在確認
・回答一覧の取得
・未読と既読の更新
・ステータスの更新
・重要マークの更新
・管理者用メモの更新
・回答の絞り込み

URLなどから渡されたuserIdを信用せず、必ずログインセッションから取得したuserIdを使用してください。

==================================================
20. OAuthトークンの保存
==================================================

Google Drive APIとGoogle Forms APIへ継続してアクセスするため、OAuthトークンを安全に管理してください。

必要に応じて以下を保存してください。

・access_token
・refresh_token
・token_uri
・client_id
・scopes
・expiry

ただし、OAuthトークンを平文のまま不用意にFirestoreへ保存しないでください。

安全な保存方法を設計してください。

最低限、以下を守ってください。

・ログへトークンを出力しない
・画面へトークンを表示しない
・APIレスポンスへトークンを含めない
・Gitへトークンを含めない
・必要最小限のスコープを使用する
・本番環境では暗号化を検討する
・トークン更新失敗時は再ログインを案内する

==================================================
21. 環境変数
==================================================

.envで以下を管理してください。

SECRET_KEY=
FLASK_ENV=
APP_BASE_URL=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

FIREBASE_PROJECT_ID=
FIREBASE_CREDENTIALS_PATH=

MOCK_MODE=false

SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

ローカル開発ではSESSION_COOKIE_SECUREをfalseに変更できるようにしてください。

FIREBASE_CREDENTIALS_PATHには、FirebaseサービスアカウントJSONのパスを設定してください。

ローカル例：

FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json

VPS例：

FIREBASE_CREDENTIALS_PATH=/var/www/mitokiya/secrets/firebase-service-account.json

.env.exampleも作成してください。

.env.exampleには実際の秘密情報を記載しないでください。

==================================================
22. .gitignore
==================================================

以下を.gitignoreへ追加してください。

.env
firebase-service-account.json
secrets/
venv/
.venv/
__pycache__/
*.pyc
.DS_Store

サービスアカウントJSON、OAuthクライアントシークレット、トークン情報をGitHubへアップロードしないでください。

すべてのJSONファイルを除外すると、必要なJSONファイルまで除外される可能性があるため、*.jsonを一括指定しないでください。

==================================================
23. Google APIのスコープ
==================================================

Google APIの権限は、必要最小限にしてください。

Google Drive APIについては、Googleフォーム一覧取得に必要な範囲だけを要求してください。

Google Forms APIについては、フォーム情報と回答取得に必要な範囲だけを要求してください。

実装時に、使用するスコープをREADMEへ明記してください。

不要なGoogle Drive全体への編集権限や削除権限は要求しないでください。

==================================================
24. モックモード
==================================================

Google APIとFirebaseの設定が完了していない状態でも、UIを確認できるモックモードを用意してください。

.envに以下を設定した場合、モックモードで起動してください。

MOCK_MODE=true

モックモードでは以下の処理を行ってください。

・Googleログインを省略できる
・Firebaseへ接続しない
・Google Drive APIへ接続しない
・Google Forms APIへ接続しない
・ダミーのフォームと問い合わせを表示する
・ステータス変更を画面上で確認できる
・未読、既読の切り替えを確認できる
・重要マークを確認できる
・管理者メモを確認できる

モックデータには以下を含めてください。

フォーム：

・u-shin お問い合わせフォーム
・はよおきんかい フィードバック
・まとめときや フィードバック

問い合わせ：

・未読の問い合わせ
・既読の問い合わせ
・未対応の問い合わせ
・対応中の問い合わせ
・対応済みの問い合わせ
・保留の問い合わせ
・重要な問い合わせ
・管理者メモがある問い合わせ
・メールアドレスが取得できない問い合わせ

モックモードと本番モードで、テンプレートを大きく分けないでください。

同じUIを使い、データ取得部分だけを切り替える構成にしてください。

==================================================
25. UIデザイン
==================================================

Google Formsの画面をそのままコピーせず、問い合わせ管理ツールらしいデザインにしてください。

デザイン方針：

・白をベースにする
・紫をアクセントカラーにする
・シンプルで見やすくする
・角丸のカードを使用する
・余白を十分に取る
・デスクトップ表示を優先する
・問い合わせの状態が一目で分かる
・日本語UIにする
・Googleのサービスと分かる程度の親和性を持たせる
・Google Formsの完全な模倣にはしない
・派手なアニメーションを多用しない
・操作結果を分かりやすく表示する

ステータスはバッジで表示してください。

例：

・未対応
・対応中
・対応済み
・保留

未読の回答は目立つようにしてください。

重要な問い合わせには星または重要アイコンを表示してください。

回答内容が長い場合は一覧では省略し、詳細で全文を表示してください。

==================================================
26. 非同期処理
==================================================

以下の操作は、可能な限りページ全体を再読み込みせずに処理してください。

・既読への変更
・ステータス変更
・重要マーク変更
・メモ保存
・回答同期
・検索
・絞り込み

fetch APIを使用してFlaskのAPIエンドポイントへ送信してください。

APIレスポンスはJSON形式にしてください。

例：

{
  "success": true,
  "message": "対応状況を更新しました",
  "data": {
    "status": "completed"
  }
}

エラー時は以下のようにしてください。

{
  "success": false,
  "message": "対応状況を更新できませんでした"
}

内部エラーの詳細を、そのままブラウザへ表示しないでください。

==================================================
27. エラー処理
==================================================

以下のケースを考慮してください。

・Googleログインに失敗した
・OAuthのstateが一致しない
・OAuthトークンの有効期限が切れた
・refresh_tokenが取得できない
・Google APIの権限が不足している
・フォームへのアクセス権がなくなった
・フォームが削除された
・フォームが管理対象から外れた
・Google Forms APIが一時的に失敗した
・Google Drive APIが一時的に失敗した
・Firebaseへの接続に失敗した
・サービスアカウントJSONが存在しない
・回答が0件だった
・管理対象フォームが0件だった
・質問が削除または変更された
・回答者名を取得できない
・メールアドレスを取得できない
・同じ回答を再取得した
・Firestoreの複合インデックスが不足している
・メモの保存に失敗した
・ステータス更新に失敗した
・一部のフォームだけ同期に失敗した

ユーザーには分かりやすい日本語で表示してください。

例：

・「Googleアカウントへの接続が切れました。もう一度ログインしてください」
・「このフォームを閲覧する権限がありません」
・「回答の取得に失敗しました。時間を空けて再度お試しください」
・「管理対象のフォームが登録されていません」
・「まだ回答は届いていません」

サーバーログには、原因を確認できる情報を残してください。

ただし、以下をログへ出力しないでください。

・アクセストークン
・リフレッシュトークン
・クライアントシークレット
・サービスアカウントJSONの内容
・問い合わせ本文の全文
・個人情報

==================================================
28. セキュリティ
==================================================

以下のセキュリティ対策を行ってください。

・FirebaseサービスアカウントJSONをGitHubへアップロードしない
・OAuthクライアントシークレットをソースコードへ書かない
・SECRET_KEYを環境変数で管理する
・CSRF対策を行う
・OAuthのstateを検証する
・ユーザーごとにFirestoreのデータパスを分離する
・他のユーザーのフォームや回答を閲覧できないようにする
・すべてのデータ操作でログイン中ユーザーを確認する
・URLパラメータのuserIdを信用しない
・回答内容をHTMLへ出力するときはエスケープする
・XSS対策を行う
・本番環境ではHTTPSを使用する
・Secure Cookieを使用する
・HttpOnly Cookieを使用する
・SameSite属性を設定する
・必要以上に広いGoogle APIスコープを要求しない
・APIエンドポイントでも認証状態を確認する
・不正なformIdやresponseIdを検証する
・他人のデータパスを指定できないようにする

Firestoreの保存先は必ず以下のようにユーザー単位で分離してください。

users/{ログインユーザーID}/managed_forms/...

クライアント側JavaScriptからFirestoreを直接操作せず、基本的にFlaskのバックエンドを経由してください。

Firebase Admin SDKはFirestore Security Rulesを経由しないため、Flask側でユーザーIDとデータ所有者を必ず確認してください。

==================================================
29. Firestoreインデックス
==================================================

以下の条件を組み合わせた検索で、Firestoreの複合インデックスが必要になる可能性があります。

・statusとsubmitted_at
・is_readとsubmitted_at
・is_importantとsubmitted_at
・formIdとsubmitted_at

必要な複合インデックスがある場合は、READMEへ記載してください。

firebase.indexes.jsonを使用する構成にする場合は、そのファイルも作成してください。

ただし、Firebase CLIを導入しなくても基本機能を動かせるようにしてください。

==================================================
30. ファイル構成
==================================================

機能を1つのファイルへ詰め込まず、以下のように分割してください。

app/
  __init__.py
  config.py
  firebase_config.py
  extensions.py

  auth/
    __init__.py
    routes.py
    google_oauth.py
    decorators.py

  dashboard/
    __init__.py
    routes.py

  forms/
    __init__.py
    routes.py
    google_drive_api.py
    google_forms_api.py
    sync_service.py

  responses/
    __init__.py
    routes.py
    response_service.py

  repositories/
    __init__.py
    user_repository.py
    form_repository.py
    response_repository.py

  services/
    __init__.py
    answer_parser.py
    count_service.py

  mock/
    __init__.py
    mock_data.py
    mock_repository.py

  templates/
    base.html
    login.html
    dashboard.html
    forms.html
    settings.html
    errors/
      400.html
      403.html
      404.html
      500.html

  static/
    css/
      style.css
    js/
      dashboard.js
      forms.js
      api.js

run.py
requirements.txt
.env.example
.gitignore
README.md

必要に応じて構成を改善して構いません。

ただし、以下を守ってください。

・Google API処理と画面処理を分離する
・Firestore処理とルート処理を分離する
・同期処理を専用サービスへ分離する
・回答解析を専用サービスへ分離する
・同じ処理を複数ファイルへ重複して書かない
・循環importが起きない構成にする

==================================================
31. requirements.txt
==================================================

requirements.txtには、最低限以下を含めてください。

Flask
gunicorn
python-dotenv
firebase-admin
google-auth
google-auth-oauthlib
google-auth-httplib2
google-api-python-client
Flask-WTF
requests
cryptography

使用していないライブラリを大量に追加しないでください。

SQLite、Flask-SQLAlchemy、SQLAlchemy関連のライブラリは追加しないでください。

==================================================
32. README
==================================================

README.mdには以下を記載してください。

・アプリの概要
・解決する課題
・主な機能
・画面構成
・使用技術
・ディレクトリ構成
・必要なPythonバージョン
・ローカルでの起動方法
・仮想環境の作成方法
・requirements.txtのインストール方法
・Firebaseプロジェクトの作成方法
・Cloud Firestoreの有効化方法
・Firebaseサービスアカウントの発行方法
・サービスアカウントJSONの設定方法
・Google Cloud Consoleでの設定方法
・Google Drive APIの有効化方法
・Google Forms APIの有効化方法
・OAuth同意画面の設定方法
・OAuthクライアントIDの作成方法
・リダイレクトURIの設定方法
・使用するGoogle APIスコープ
・.envの設定例
・モックモードの使用方法
・Firestoreのデータ構成
・必要なFirestore複合インデックス
・Gunicornでの起動方法
・Nginxの設定例
・systemdサービスの設定例
・VPSへのデプロイ方法
・Firebase認証情報を安全に配置する方法
・Google APIとの接続が切れた場合の対処方法
・よくあるエラーと対処方法

READMEへ実際の秘密情報を記載しないでください。

==================================================
33. 起動コマンド
==================================================

ローカルでは以下のように起動できるようにしてください。

python run.py

または、

flask --app run.py run --debug

本番環境では以下のようにGunicornで起動できるようにしてください。

gunicorn --workers 3 --bind 127.0.0.1:8003 run:app

ポート番号は既存VPS上のアプリと重複しないように確認してください。

==================================================
34. 実装順序
==================================================

以下の順番で実装してください。

1. 既存プロジェクトの確認
2. Flaskの基本構成
3. 環境変数とconfigの作成
4. モックモードの作成
5. モックデータによる受信箱UIの作成
6. ステータス、未読、重要、メモのUI実装
7. Firebase Admin SDKの初期化
8. Firestore Repositoryの作成
9. Firestoreへのステータス、未読、重要、メモ保存
10. Google OAuthの実装
11. Google Drive APIによるフォーム一覧取得
12. 管理対象フォームの登録
13. Google Forms APIによるフォーム情報取得
14. Google Forms APIによる回答取得
15. 質問タイトルと回答内容の変換処理
16. Firestoreへの回答同期
17. responseIdによる重複防止
18. 複数フォームの回答一括表示
19. 検索と絞り込み
20. 件数集計
21. エラー処理
22. セキュリティ確認
23. README作成
24. ローカルでの動作確認
25. 本番環境向け設定の確認

最初からすべてを一気に実装して、動かない状態にしないでください。

各段階で動作確認してください。

まずはMOCK_MODE=trueでUIと基本操作を完成させ、その後FirebaseとGoogle APIを接続してください。

==================================================
35. MVPでは実装しない機能
==================================================

最初の段階では、以下の機能は実装しないでください。

・Googleフォーム自体の作成
・Googleフォーム自体の編集
・Googleフォームの質問追加
・回答者への自動返信
・Gmailとの自動連携
・LINE通知
・Slack通知
・Discord通知
・AIによる回答要約
・AIによる緊急度判定
・チーム共同管理
・担当者割り当て
・複雑な権限管理
・有料プラン
・決済
・高度なグラフ分析
・外部全文検索サービス
・スマートフォンアプリ

まずは以下を完全に動作させてください。

Googleログイン
↓
フォームを選択
↓
回答を同期
↓
すべての回答を一覧表示
↓
問い合わせを開く
↓
未読から既読へ変更
↓
対応状況を変更
↓
重要マークを変更
↓
管理者用メモを保存

==================================================
36. 動作確認
==================================================

最低限、以下を確認してください。

・MOCK_MODE=trueで起動できる
・Firebase認証情報がなくてもモックモードでは起動できる
・Google APIの設定がなくてもモックモードでは起動できる
・ログイン画面が表示される
・Googleログイン後にダッシュボードへ移動できる
・Googleフォーム一覧を取得できる
・管理対象フォームを登録できる
・複数フォームの回答を取得できる
・同じ回答が重複登録されない
・回答が新しい順に表示される
・問い合わせを開くと既読になる
・ステータスを変更できる
・重要マークを変更できる
・メモを保存できる
・検索できる
・絞り込みできる
・他のユーザーのデータを閲覧できない
・Google APIのエラー時に画面が壊れない
・Firestore接続エラー時に分かりやすい表示になる
・スマートフォンでも最低限操作できる

==================================================
37. 実装後の報告
==================================================

実装が完了したら、以下を報告してください。

1. 作成したファイル一覧
2. 変更したファイル一覧
3. 実装した機能
4. 未実装の機能
5. ローカル起動コマンド
6. 本番起動コマンド
7. 必要な環境変数
8. Google Cloud Console側で必要な設定
9. Firebase側で必要な設定
10. Firestoreのデータ構成
11. 必要なFirestoreインデックス
12. 動作確認した項目
13. 現在発生している問題
14. 次に実装すべき機能

コードを作成しただけで完了とせず、可能な範囲で実際に起動し、エラーを確認して修正してください。

==================================================
38. 最重要事項
==================================================

このアプリのトップページは、Googleフォームの一覧画面ではありません。

管理対象のすべてのGoogleフォームに届いた回答を、問い合わせの受信箱として一括表示する画面にしてください。

ユーザーがGoogle Formsを毎回1つずつ開かなくても、以下を1画面で完了できることを最優先にしてください。

・新着問い合わせの確認
・問い合わせ内容の確認
・未読と既読の管理
・対応状況の変更
・重要マークの変更
・管理者メモの保存

機能を増やすことよりも、毎日の問い合わせ確認作業が本当に簡単になることを優先してください。